version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENV=development
      - FIREBASE_CREDENTIALS_PATH=/app/secrets/firebase-service-account.json
    volumes:
      - .:/app
    restart: always
